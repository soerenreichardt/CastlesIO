export interface LobbySettings {
    turnTimeSeconds: number;
    maxPlayers: number;
    tileList: number[];
    gameMode: string;
    gameModes: string[];
    visibility: string;
    visibilities: string[];
    editable: boolean;
}
